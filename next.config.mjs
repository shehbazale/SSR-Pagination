/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "placeimg.com",
      "images.unsplash.com",
      "i.imgur.com",
      "images.pexels.com",
      "i.imgur.com",
    ],
  },
};

export default nextConfig;
